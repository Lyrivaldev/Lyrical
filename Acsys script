local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/ShexYt/L0c0N/main/cat%20(1).txt"))() --you can go into the github link and copy all of it and modify it for yourself.
local Window = Library:CreateWindow("Acsy.cc | ☫", Vector2.new(300, 300), Enum.KeyCode.RightControl) --you can change your UI keybind
local AimingTab = Window:CreateTab("Legit") --you can rename this tab to whatever you want --you can also change the tabs code, for example "AimingTab" can be changed to "FunnyCoolTab" etc.


local testSection = AimingTab:CreateSector("First Section", "left")

testSection:AddButton("Camlock", function(IhateGayPeople)
   loadstring(game:HttpGet("https://raw.githubusercontent.com/Lyrivaldev/Lyrical/main/Lock%20acsy"))()
    print("button")
end)

testSection:AddTextbox("Prediction", nil, function(State)
end)

local testSection = AimingTab:CreateSector("First Section", "right")

testSection:AddButton("Silent aim", function(IhateGayPeople)
   loadstring(game:HttpGet("https://raw.githubusercontent.com/Lyrivaldev/Lyrical/main/Streamble"))()
    print("button")
end)

testSection:AddTextbox("Prediction", nil, function(State)
end)

local AimingTab = Window:CreateTab("Rage")


local testSection = AimingTab:CreateSector("First Section", "left")  --you can  change the section code, for example "testsection" can be changed to "FunnyCoolSection" etc.

testSection:AddToggle("Toggle", false, function(first)
    print("ejejejejejeje")
end)

testSection:AddButton("Blantant silent", function(IhateGayPeople)
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Lyrivaldev/Lyrical/main/Silent%20aim"))()
    print("button")
end)

testSection:AddSlider("Silent fov", 0, 120, 2000, 1, function(State)
    
end)

testSection:AddTextbox("prediction", nil, function(State)

end)

testSection:AddDropdown("hit body", {"head", "torso", "lowertorso", "humanoid"}, "selected", true, function(dropdown)

end)

testSection:AddButton("antilock", function(IhateGayPeople)
   loadstring(game:HttpGet("https://raw.githubusercontent.com/Lyrivaldev/Lyrical/main/Anti%20lock"))()
    print("button")
end)

ColorToggle:AddColorpicker(Color3.fromRGB(255, 0,0), function(ztx)
   
end)

local ToggleBind = testSection:AddToggle("Keybind w/Toggle", false, function(e)

end)

ToggleBind:AddKeybind()

AimingTab:CreateConfigSystem("right") --this is the config system
